# ARIM RDE Tool - Release Notes v2.3.14

**リリース日**: 2025年12月30日

---

## 概要

v2.3.14 は、低頻度で発生する UI の黒い未塗り領域（表示の黒化）を抑止する改善と、
RDE 側で削除済み（404/410 確定）のデータセット/サブグループIDを候補から除外して、ローカルJSONとの乖離によるエラーを回避する改善を含むリリースです。
加えて、pytest 実行時の固まり（モーダル/ネットワーク起因）を抑制し、規約準拠の整理を行いました。

---

## 変更点（概要）

### 🎨 UI/テーマ

- ウィジェット切替などで低頻度に発生する「意図しない黒い領域」を抑止するため、`QScrollArea` の viewport 背景へテーマ色を直接適用する仕組みを追加。
  - 透明/未描画領域が黒にフォールバックする経路を低減。

### 🧩 データ整合性（RDE側削除データの除外）

- RDE API で 404/410 が確定したデータセット/サブグループIDをローカルに記録し、以後コンボ候補から除外。
  - ローカルJSONとRDEの乖離により編集途中でサーバ側エラーになるケースを、UI側で早期に抑止。

### 🧪 テスト/品質

- pytest 実行時にモーダルダイアログやネットワークアクセスで固まらないようガードを整備。
- `src/` 配下での `unittest.mock` 依存（規約違反）を解消。

---

## ドキュメント/配布物の版数表記

- `VERSION.txt` / `README.md` / `docs/CHANGELOG.md` / `docs/RELEASE_NOTES_v2.3.14.md` 等 → v2.3.14
