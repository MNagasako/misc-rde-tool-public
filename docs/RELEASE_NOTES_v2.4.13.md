# RELEASE NOTES v2.4.13

v2.4.13 は、データセット一覧タブのフィルタUI改善と、絞り込み/エクスポートの整合性改善を含むリリースです。

## 変更点

- データセット一覧タブ
  - 範囲フィルタ群を2行表示に変更（フィルタ領域の視認性改善）。
  - TAG数の範囲フィルタが実際に絞り込みに反映されない不具合を修正。

- エクスポート
  - `file_size` は bytes(int) を保持し、エクスポートでも bytes を出力するよう統一。

## テスト

- ユニット/ウィジェット/統合テストを実行し、回帰を防止。
