# ARIM RDE Tool - Release Notes v2.4.7

**リリース日**: 2026年1月6日

---

## 概要

v2.4.7 は、メール設定の「上書きインストール耐性」強化、更新後の再起動フロー改善、および Windows + PySide6 + pytest-qt 環境でのテスト安定化を含む改善リリースです。

---

## 変更点（概要）

### ⚙️ 設定/メール

- メールタブの非機微設定（From/SMTP host 等）を OS キーチェーンへ退避し、上書きインストール等で設定 JSON が初期化されても自動復元できるように改善。

### 🧩 UI

- 設定画面の MISC タブをタブ一覧の最後尾へ移動。

### 🔄 更新

- MISC タブのアプリ更新後に自動で再起動するフローを追加（インストーラ完了待ち→再起動を別プロセスへ委譲）。

### 🧪 テスト/品質（安定性）

- Windows + PySide6 + pytest-qt の長時間実行で発生し得るクラッシュを回避するため、描画監視のイベントフックを root 配下に限定して安定化。

---

## ドキュメント/配布物の版数表記

- `VERSION.txt` / `README.md` / `docs/CHANGELOG.md` / `docs/RELEASE_NOTES_v2.4.7.md` 等 → v2.4.7

---

## 互換性

- 既存機能の互換性を維持しつつ、運用・安定性を改善しています。
